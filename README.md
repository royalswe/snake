Multiplayer snake game made with uWebsocket.js and sveltekit 2.0

Navigate to both server and client folder and install dependencies
`pnpm install`

start each client and server or run from root to start both
`npm run dev`
